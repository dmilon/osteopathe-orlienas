backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'post' }
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
  - name: 'pages'
    label: 'Pages'
    folder: '_pages'
    create: true
    slug: '{{title}}'
    editor:
      preview: false
    fields:
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'page' }
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Card image source', name: 'card_img_src', widget: 'string' }
      - { label: 'Card image description', name: 'card_img_alt', widget: 'string' }
      - { label: 'Page image source', name: 'page_img_src', widget: 'string' }
      - { label: 'Page image description', name: 'page_img_alt', widget: 'string' }
      - { label: 'Position', name: 'position', widget: 'number' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
